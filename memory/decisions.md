# Decision Log

> Append-only via `scripts/append-decision.sh "<text>"` (date prepended automatically).
> Root-owned in production (root:pi 0664) so the agent can only append through the wrapper.

(no decisions yet)
