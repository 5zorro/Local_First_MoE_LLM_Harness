# Ralph Loop Goal

_Fill this with a single, long, unattended objective, then run `scripts/ralph-loop.sh`.
The loop iterates (plan → act → checkpoint) until it writes a DONE marker. Keep ordinary
prompts OUT of the loop — this is for genuinely long grinds only._

## Goal
(describe the objective + a clear done-condition here)
